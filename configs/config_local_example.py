# Copy this file to `configs/config_local.py` (which is gitignored) and edit paths.

# Option A: set a default base dir used when no env var is provided.
DEFAULT_BASE_DIR = "/path/to/cubicasa"

# Option B: force BASE_DIR (overrides DEFAULT_BASE_DIR).
# BASE_DIR = "/path/to/cubicasa"

# If you leave both values unset, the repo will automatically reuse the
# published CubiCasa5k-ScanShift release from its default cache once fetched.
